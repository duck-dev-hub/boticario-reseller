import React from 'react';
import Burgue from './styles';

const IconBurgue = ({ onClick, className }) => {
  return <Burgue onClick={onClick} className={className} />;
};

export default IconBurgue;
